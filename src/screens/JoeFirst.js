import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const JoeFirst = () => {
  return (
    <View>
      <Text> "Hello, World!"</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default JoeFirst;
